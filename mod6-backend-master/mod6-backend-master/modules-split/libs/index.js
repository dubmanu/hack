'use strict';

const operation2String = require('./operations-mapper');
const operations = require('./operations');
const port = require('./port');
const sum = require('./sum');

module.exports = {
  operation2String,
  operations,
  port,
  sum,
};
